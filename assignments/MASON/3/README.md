# Assignment 3 – Cookie Report (100 Sites)

This report analyzes cookie practices for my 100 assigned sites (subset of Mozilla’s Top 500).  
All data was collected with `curl -I -L -k` and parsed by a Python script.  
Raw headers are in `./responses/`.

## Summary Table
(Paste the table from results.md here)

## Answers
- **HttpOnly cookies:** (number from stats)
- **Secure cookies:** (number from stats)
- **SameSite:** Strict = X, Lax = Y, None = Z
- **Path:** “/” = A, Other paths = B
- **Min/Max/Mean/Median:** (values from stats.txt)
